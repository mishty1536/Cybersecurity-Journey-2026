1. we can use Replit to execute JS codes 
2. JS is dynamic programming language. we can change the datatype of variables during execution 
3. const vs let vs var(ES6)
-> var is global and const , let are block specific 
-> const will not be changed, should be initialized before
-> var , let can be initialized afterwards
4. Datatypes and objects
-> objects are key-value pairs
-> datatypes are of 2 types Premitive(7) and Non-Premitive(1)
-> Premitive (nnbbssu) = Null, Number, Byte, Bool, String, Symbol, Undefined
-> Non-Premitive is object
-> in object we can add a new key value pair also we can change any existing key's value
4. Expression and Operators
-> Arithmetic(+,-,/,%,*), Assignment(=,==,+=,-=),Logical(||,&&,!),ternary
5. COnditional statements
-> if, if-else, if-elseif-else, switch
6. Loops 
-> For loop: for(let i=0; i<n; i++)
-> For In Loop: for(let a in obj) can be used for array, it print keys of object 
-> For Of Loop: for(let a of obj) used for arrays, here object should be iterable 
--> let ka scope sirf loop tak ka hota hai so use let and not var. After loop if you are ased value of th variable used that be not defined if used let in loop and if var is used will get value of that variable
-> while loop: while(condition){}
->Do-while Loop: do{

}while(condition) fist block runs then condition is checked so atleast one time it will run
7. Functions
->block of code used to perform a particular task
-> syntax: function func.Name(parameter1, parameter2){
    //code
}
->Another syntax: const func.Name = (Parameters) => {} this is using arrow function
